name: CI

on:
  push:
    branches: [main]

  workflow_dispatch:

jobs:
  build-and-pack:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Restore deps
        run: yarn install

      - name: Build extension
        run: yarn build
